/**
 * Planning orchestration boundary.
 *
 * <p>Plan contracts live in {@code model}; creation and protocol normalization in
 * {@code generation}; candidate comparison in {@code selection}; runtime constraints in
 * {@code validation}; revision auditing in {@code evolution}; execution lifecycle handling in
 * {@code execution}; and durable plan state in {@code snapshot}.</p>
 */
package com.chatchat.agents.orchestration.planning;
