/**
 * Runtime OS plan kernel and public plan protocol.
 *
 * <p>This root package is intentionally closed to new responsibility classes. Candidate
 * admission belongs to {@code selection}, diagnostic lifecycle belongs to {@code diagnostic},
 * durable contracts belong to {@code persistence}, and DAG passes belong to
 * {@code transformation}. The remaining root types are the plan model, execution kernel and
 * migration-era compiler/repair collaborators.</p>
 */
package com.chatchat.agents.runtime.plan;
