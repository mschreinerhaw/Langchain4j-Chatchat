package com.chatchat.common.knowledge.template.matching;

/** Semantic coverage opinion emitted by the model and enforced by Runtime. */
public enum TemplateCoverageDecision {
    SUFFICIENT,
    NEED_NEXT_PAGE,
    SCOPE_INSUFFICIENT
}
