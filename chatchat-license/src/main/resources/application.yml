server:
  port: ${CHATCHAT_LICENSE_CENTER_PORT:8092}
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  application:
    name: chatchat-license-center
  datasource:
    url: ${CHATCHAT_LICENSE_DB_URL:jdbc:h2:file:./data/license-center/license-audit;MODE=MySQL;DATABASE_TO_LOWER=TRUE;AUTO_SERVER=TRUE}
    username: ${CHATCHAT_LICENSE_DB_USERNAME:sa}
    password: ${CHATCHAT_LICENSE_DB_PASSWORD:LiveMCP_H2#Audit@2026!Secure}
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: always
      encoding: UTF-8

chatchat:
  license-center:
    private-key-path: ${CHATCHAT_LICENSE_PRIVATE_KEY_PATH:./data/license-center/license-private.pem}
    public-key-path: ${CHATCHAT_LICENSE_PUBLIC_KEY_PATH:./data/license-center/license-public.pem}
    auto-generate-keys: ${CHATCHAT_LICENSE_AUTO_GENERATE_KEYS:true}
    key-id: ${CHATCHAT_LICENSE_KEY_ID:default}
    username: ${CHATCHAT_LICENSE_CENTER_USERNAME:license-admin}
    password: ${CHATCHAT_LICENSE_CENTER_PASSWORD:123456}
    mcp-menu-catalog-url: ${CHATCHAT_MCP_MENU_CATALOG_URL:http://localhost:8090/api/v1/license/menu-catalog}
    mcp-menu-catalog-timeout-ms: ${CHATCHAT_MCP_MENU_CATALOG_TIMEOUT_MS:5000}
