# Development plugin libraries

Place optional MCP shared dependency jars here when starting the development services with
`run-chat-api-mcp.ps1` or `run-chat-api-mcp.bat`.

Restart the MCP process after changing jars. Release packages use their own `lib/plugins/`
directory with the same loading contract.
