spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: "jdbc:mysql://127.0.0.1:3306/live_runtime_mcp?useUnicode=true&characterEncoding=UTF-8&connectionCollation=utf8mb4_unicode_ci&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&connectTimeout=30000&socketTimeout=60000&tcpKeepAlive=true"
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: chatchat_mcp
    password:
    hikari:
      maximum-pool-size: 20
      minimum-idle: 2
      connection-timeout: 30000
      validation-timeout: 3000
      idle-timeout: 30000
      max-lifetime: 35000
      keepalive-time: 30000
      leak-detection-threshold: 60000
      connection-init-sql: "SET SESSION lock_wait_timeout=5, SESSION innodb_lock_wait_timeout=5"
  h2:
    console:
      enabled: false
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: false
    properties:
      jakarta.persistence.query.timeout: 30000
