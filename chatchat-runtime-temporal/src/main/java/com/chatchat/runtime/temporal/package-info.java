/**
 * Temporal adapter module boundary.
 *
 * <p>Implementations are intentionally classified below this root:</p>
 * <ul>
 *   <li>{@code config}: Spring composition and properties</li>
 *   <li>{@code core}: WorkflowRuntime adapter and definition registry</li>
 *   <li>{@code contract}: serialized Workflow/Activity messages</li>
 *   <li>{@code workflow}: deterministic orchestration</li>
 *   <li>{@code activity}: non-deterministic execution</li>
 *   <li>{@code adapter}: Agents ports backed by Temporal</li>
 * </ul>
 */
package com.chatchat.runtime.temporal;
