# 非空数据被误报为空：2026-09-05 日志复盘

本次日志中，三个 API 模板执行成功，最终结果表显示 41 行记录。资产行包含总资产 847174.25 等实际值，不能判断为数据源空结果。

19:11:35 统一分析模型调用完成（190670 ms），紧接着出现 `Invalid bounded evidence range`。补证读取的参数错误冒泡为整次分析失败；`SemanticGapEvidenceBridge` 捕获后返回空分析产物。19:11:36 最终综合仍启动，记录 `claimBoundPublication=false`、`admittedClaimCount=0`，最终正文错误宣称未返回业务数据，而独立的表格链路仍展示原始记录。

日志没有记录该次补证参数，不能断言具体是起始行、超大 limit 或哪个数据集有误。确定的根因是：可恢复的补证参数错误造成全局分析丢失，以及分析失败后仍允许无结论约束的自由综合。

修复：

- 单项补证参数错误返回 `REQUEST_REJECTED`、原因和实际可用行数；保留其他成功读取。
- 若已有候选 Findings 且全部可选读取被拒绝，保留候选并执行原有证据校验，附加未解决限制。
- 若尚无 Findings，把拒绝信息交回模型纠正，沿用既有三轮预算，不无限重试。
- 数据投影时记录 `analysisObservedReturnedRecordCount`。分析失败且原始记录非空时，停止最终自由综合，明确失败；不将错误转换为空数据报告。
- 成功预检清除此前的失败标识，避免恢复后被旧状态误拦截。
- 补证审计记录轮次、操作、数据集、请求起始行/数量、可用行数和拒绝原因；日志不回放业务行内容。每次分析重新初始化审计和拒绝计数。
- 即使没有抛异常，合法协议返回零 Findings 且原始记录非空时，也停止自由综合；不能把“没有分析结论”当成“数据源空结果”。

回归覆盖 41 行非空数据、非法补证范围、候选保留、无候选时请求纠正、同批成功读取与失败读取隔离，以及异常/零 Findings 时最终模型零调用。该修复不构成真实模型报告质量或时延的生产验收，部署后仍需重放原请求。
