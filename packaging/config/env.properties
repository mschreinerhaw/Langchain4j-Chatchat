# Optional JDK location. Uncomment and use the actual JDK 17 installation path.
# JAVA_HOME=/opt/jdk-17

# JVM startup options. The packaging startup scripts read this assignment.
JAVA_OPTS=-Xms2g -Xmx4g -XX:+UseG1GC -XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=./logs -XX:+ExitOnOutOfMemoryError -Dfile.encoding=UTF-8

# Optional Spring application overrides can also be placed in this file.
# Use regular Spring property names and bracket notation for model map keys.
# Values here override application*.yml, while command-line/system/environment values remain higher priority.
#
# chatchat.models.default-chat-model=internal-model
# chatchat.models.available-chat-models[0]=internal-model
# chatchat.models.chat-models[internal-model].model-name=provider-model-id
# chatchat.models.chat-models[internal-model].api-key=${CHATCHAT_INTERNAL_MODEL_API_KEY:no-key}
# chatchat.models.chat-models[internal-model].base-url=http://127.0.0.1:31000/v1
# chatchat.models.chat-models[internal-model].protocol=openai
# chatchat.models.chat-models[internal-model].timeout=1800000
# chatchat.models.chat-models[internal-model].max-tokens=-1
# chatchat.models.chat-models[internal-model].max-retries=1
