# Runtime libraries

The application jar is packaged into `lib/app`.

Put optional non-JDBC application library jars under `lib/ext`, or directly under `lib`.

Place optional JDBC drivers under `lib/drivers` when using database integrations that are not bundled in the application.
